PORT=8080
DATABASE_URL=postgres://postgres:postgres@localhost:5432/notifyhub?sslmode=disable
REDIS_URL=redis://localhost:6379

# SMTP (optional — uses LogSender if not set)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@notifyhub.local
